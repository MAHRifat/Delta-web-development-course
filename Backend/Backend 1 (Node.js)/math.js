const sum = (a, b) => a+b;
const mul = (a, b) => a*b;
const g = 9.8;
const pi = 3.1416;

let obj = {
    sum: sum,
    mul: mul,
    g: g,
    pi: pi
};

module.exports = obj;   // export something using this command

// exports = 1233;  // not valid



// Terminal -> a text input output environment

// Different Terms
    // command line -> any interface that is used by entering textual commands (gen. Windoes centric)

    // Terminal -> This is a tye of command line (gen. Mac centric)

    // console -> A command line interface used to work with your computer

    // shell -> A promgram running on terminal

    // Bash -> A popular shell on Mac OS/Linux

    // Z-Shell -> Another shell (default)


// terminal basic command
    // ls -> list files (show my files)
    // pwd -> print working director (where am i?)
    // clear -> clear screen
    // cd -> change direcotry ----------cd CSS (go CSS folder) --cd .. (back to previous folder)
    // cd / -> root directory
    // cd ~ -> home directory
    // mkdir -> make direactory
    
    // Flags -> flags are characters that we pass whith commands to modify their behaviour
        // manual command
            // man ls -> give info about ls command
            // man mkdir -> give info abut mkdir command
        //with flags
            // ls -l
            // ls -a
            // ls -la

    // create new file 
        // touch index.html
        // touch app.js


    // deleting files and folder
        // rm -> removes files
        // rmdir -> removes empty folders
        // rm -rf -> removes any folders

        